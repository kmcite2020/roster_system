part of 'domain.dart';

// @freezed
// class Manager with _$Manager {
//   const factory Manager({
//     @Default('NONE') final String name,
//     required final String id,
//   }) = _Manager;
//   factory Manager.fromID(String id) => domainBloc.managers[id] != null ? domainBloc.managers[id]! : Manager(id: id);
//   factory Manager.fromJson(json) => _$ManagerFromJson(json);
// }
