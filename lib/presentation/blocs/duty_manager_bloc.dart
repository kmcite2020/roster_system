final dutyManagerBloc = DutyManagerBloc();

class DutyManagerBloc {
  // final dutyManagerRM = RM.inject(
  //   () => Manager(id: randomID),
  //   // persist: () => PersistState(
  //   //   key: DutyManager.key,
  //   //   fromJson: (json) => DutyManager.fromJson(jsonDecode(json)),
  //   //   toJson: (s) => jsonEncode(s.toJson()),
  //   // ),
  // );

  // Manager get getDutyManager => dutyManagerRM.state;
  // void setDutyManager(Manager dutyManager) {
  //   dutyManagerRM.state = dutyManager;
  // }

  void setDutyManagerName(String name) {
    // setDutyManager(
    //   getDutyManager.copyWith(name: name),
    // );
  }
}
