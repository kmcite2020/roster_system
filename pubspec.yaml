name: roster_system
description: A new Flutter project for Managing rosters here at BKMC Swabi.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons:
  colornames:
  path_provider: ^2.1.1
  flex_color_scheme: ^7.3.1
  freezed_annotation:
  json_annotation:
  manager:
    path: D://FLUTTER//workspace_ui//packages//manager
  extensions:
    path: D://FLUTTER//workspace_ui//packages//extensions
dev_dependencies:
  build_runner: ^2.4.6
  freezed:
  json_serializable:

flutter:
  uses-material-design: true
